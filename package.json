{
  "name": "ether-freakers",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ ether-freakers",
    "create-local": "graph create --node http://localhost:8020/ ether-freakers",
    "remove-local": "graph remove --node http://localhost:8020/ ether-freakers",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 ether-freakers"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.21.1",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
